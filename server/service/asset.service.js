const axios = require("axios");
const { EXTERNAL_ASSET_API_BASE_URL } = require('../global_variable');

const dummy = () => {
  return EXTERNAL_ASSET_API_BASE_URL
}

const getItemIcon = async (itemId) => {
  
}

module.exports = {
  dummy
};
